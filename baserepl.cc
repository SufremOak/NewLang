#include <iostream>
#include <filesystem>
#include <ncurses.h>
#include <string>
#include <filesystem>
#include <fstring>

public:
  void InterpretCommand(int args) {
    while(1) {
      printf();
    }
  }

int main() {}
